PROGRAM_NAME = platformer

LIBS_EXTERN = libprocps zlib freealut glew glfw3 $(shell pkg-config --print-requires --print-requires-private glfw3)
DEFS = -DTPS=$(TPS) -DSCALE=$(SCALE) -DSWAP=$(SWAP) -DPRINT_TO_TERMINAL=$(PRINT_TO_TERMINAL)

COMPILE_FLAGS_EXTRA +=

